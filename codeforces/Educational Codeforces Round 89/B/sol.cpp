#include <bits/stdc++.h>

using namespace std;

int main() {

  int t, n, x, m, c, d;

  cin >> t;
  while (t--) {
    cin >> n >> x >> m;
    vector<int> Arr(n+1, 0);
    Arr[x] = 1;

    while (m--) {
      
    }
  }
  return 0;
}

