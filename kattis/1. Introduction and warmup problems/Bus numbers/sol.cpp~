#include <bits/stdc++.h>
using namespace std;

int main(void) {
  int n;
  cin >> n;
  vector<int> arr(n, 0);
  for(int i = 0; i < n; i++) {
    cin >> arr[i];
  }
  for(auto i : arr)
    cout << i << " ";
  cout << '\n';
}
