#include <bits/stdc++.h>
using namespace std;

int main() {

  int t;
  while(true) {
    cin >> n;
    if(n == -1)
      break;
    int speed, time, distance = 0;
      while(n--) {
	cin >> speed >> time;
	distance += speed * t;
      }
    }
  }
}
