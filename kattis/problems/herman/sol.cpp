#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(void) {
  double r, c, a, b;
  cin >> r;
  a = M_PI * r * r;
  b = 2 * (r * r);
  cout << setprecision(6) << fixed;
  cout << a << '\n';
  cout << b << '\n';
}
