#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(void) {
  string a, b;
  cin >> a >> b;
  if (b.size() > a.size())
    cout << "no" << '\n';
  else
  cout << "go" << '\n';
}
