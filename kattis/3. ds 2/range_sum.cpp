#include<bits/stdc++.h>
using namespace std;

#define printv(arr) for (auto i : arr) cout << i << " "; cout << '\n';

int main(void) {
  // vector<int> arr = {1, 0, 7, 8, 5, 9, 3};
  // vector<int> psarr;
  // // we need to perform range queries.
  // int sum = 0;
  // for (int i = 0; i < arr.size(); i++) {
  //   sum += arr[i];
  //   psarr.push_back(sum);
  // }
  // printv(arr);
  // printv(psarr);

  int n, b;
  cin >> n >> b;
  // n *= 4;
  // while (n--) {
    
  // }
}

Name- Risav Chatterjee
Enrollment no- 166220772
Study centre code- 0534P
