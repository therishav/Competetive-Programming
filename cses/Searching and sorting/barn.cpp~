#include<bits/stdc++.h>
using namespace std;

int main(void) {
  int n, m;
  string s;
  cin >> n, m;
  while (m--) {
    cin >> s;
    if (s[0] == "+") {
    }
    else if (s[0] == "-") {
    }
    else if (s[0] == "?") {

    }
  }
  cout << st.size() << '\n';
}
