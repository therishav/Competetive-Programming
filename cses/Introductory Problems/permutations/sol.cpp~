#include<bits/stdc++.h>
using namespace std;

#define printA(A) for(auto i : A) cout << i << " "; cout << '\n';
typedef long long ll;

int main(void) {
  int n;
  cin >> n;
  if (n >= 3)
    cout << "NO SOLUTION" << '\n';
  else {
    for (int i = 2; i <= n; n+=2)
      cout << i << " ";
    for (int i
  }
}
