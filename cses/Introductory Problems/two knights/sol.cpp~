#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(void) {
  ll n;
  for (int i = 1; i <= n; i++) {
    ll a1 = i * i, a2 = a1 * (a1 - 1);
    if (k > 2)
      a2 -= 4 * (k - 1) (k - 2);
  }
}
