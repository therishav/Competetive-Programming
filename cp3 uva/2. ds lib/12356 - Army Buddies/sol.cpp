#include <bits/stdc++.h>

using namespace std;

int main() {

  int s, b, l, r;			// s no of soilders in attack line; b no of loss reports;
  while (1) {
    cin >> s >> b;
    if (s || b)
      break;

    while (b) {
      cin >> l >> r;
    }
  }
}
