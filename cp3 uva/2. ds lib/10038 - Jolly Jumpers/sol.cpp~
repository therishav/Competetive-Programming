#include <bits/stdc++.h>

using namespace std;

int main() {
  int n, val;
  cin >> n;
  vector<bool> Arr(n, 0);
  for (int i = 0; i < n; ) {
    cin >> val;
    Arr[i] = val;
  }

  for (int i = 1; i < n-1; i++) {

  }
  
  for (auto i : Arr)
    cout << i << " ";
  cout << '\n';
}

